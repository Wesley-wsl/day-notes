{
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "cd backend && npm install && npm run start"
    },
    "devDependencies": {
        "cypress": "^8.4.1"
    }
}
